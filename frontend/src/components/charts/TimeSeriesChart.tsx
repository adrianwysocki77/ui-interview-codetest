/**
 * Re-export the TimeSeriesChart component and its types from the refactored location
 */

import { TimeSeriesChart } from "./TimeSeriesChart/TimeSeriesChart";
import { TimeSeriesChartProps, DataPoint } from "./TimeSeriesChart/types";

export { TimeSeriesChart };
export type { TimeSeriesChartProps, DataPoint };
